# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[('ffmpeg.exe', '.'), ('ffplay.exe', '.'), ('ffprobe.exe', '.')],
    datas=[('config.py', '.'), ('version.py', '.'), ('user_settings.json', '.'), ('ui', 'ui'), ('utils', 'utils'), ('services', 'services'), ('models', 'models'), ('fonts', 'fonts'), ('whisper_models/tiny.pt', 'whisper_models'), ('models/vosk-model-small-en-us-0.15', 'vosk-model')],
    hiddenimports=['vosk', 'whisper_timestamped', 'moviepy', 'pydub', 'gtts', 'numpy', 'scipy', 'requests', 'bs4', 'emoji', 'cv2', 'PIL.Image', 'torch', 'torchaudio', 'transformers'],
    hookspath=['hooks'],
    hooksconfig={},
    runtime_hooks=[],
    excludes=['numba'],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='VideoGenerator_1.0.8',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['app_icon.ico'],
)
